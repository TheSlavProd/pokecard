import React from "react";
import * as S from "./styled";

// TODO: there's no need to keep this component at all

export const HeaderTitle = () => {
  return <S.HeaderTitle>PokéCARD</S.HeaderTitle>;
};
