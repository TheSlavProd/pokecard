export enum EPokemonCards {
  ADD_POKEMONS = "ADD_POKEMONS",
  ADD_CURRENT_POKEMON = "ADD_CURRENT_POKEMON",
  CHANGE_CURRENT_PAGE = "CHANGE_CURRENT_PAGE",
}

// TODO: not clear why we need this enum
export enum EConfig {
  offset = 10,
  limit = 10,
}
